# Lumen shadcn Integration

> Governance model for adopting shadcn/ui components into `@lumen/ui` as a source and behavioral-implementation layer, without shadcn ever becoming a second, competing visual theme.

## Source

- **Figma file:** Lumen AI Design System (no linked node — this integration model has no Figma source; it governs how *code-only* interaction patterns are adopted when Figma hasn't specified one)
- **Related documents:**
  - `CLAUDE.md`
  - `docs/component-architecture.md`
  - `docs/design-tokens.md`
  - `docs/roadmap.md` Phase 15
  - `docs/changelog.md`
- **Introduced:** 2026-07-23

---

# 1. Purpose

Lumen's own hand-built primitives and composites cover most enterprise-SaaS UI needs, but a few complex interaction patterns — command palettes, sheets, dropdown menus, popovers, resizable panels — are expensive to build correctly (focus management, portal behavior, full keyboard/ARIA compliance) and have no Figma-defined design of their own yet. shadcn/ui is adopted for exactly these cases: as a **source generator and behavioral layer**, not as a design system. Every visual property still resolves to an existing Lumen token.

---

# 2. The non-negotiable rule

```text
Lumen tokens are the only source of truth for:
color · typography · spacing · radius · borders · shadows · motion ·
focus states · status colors · light mode · dark mode · public component APIs

shadcn/Radix may supply:
accessible composition patterns · keyboard interaction · open/closed state
handling · ARIA behavior · component source Lumen can own and modify
```

If a shadcn-generated file's visual output cannot be traced to a real `@lumen/tokens` CSS variable, that file is not done.

---

# 3. Architecture

```text
shadcn component source (CLI-generated, cmdk/Radix behavior)
        ↓
packages/ui/src/components/internal/<name>.tsx   (adapted, Lumen-owned, no default theme)
        ↓
packages/ui/src/components/<name>/<Name>.tsx     (public wrapper — the only supported import)
        ↓
@lumen/ui  (public export)                        import { Command } from "@lumen/ui"
        ↓
shadcn-lumen-bridge.css + shadcn-tailwind-preset.cjs   (Lumen semantic tokens)
```

Applications only ever import from `@lumen/ui`. The `internal/` directory is not published API and may change shape without notice — treat any direct import from `@lumen/ui`'s internal paths in a consuming app as a bug to fix, not a supported pattern.

## 3.1 Workspace configuration

`packages/ui/components.json` configures the shadcn CLI for this repo's actual setup: Tailwind v3 (not v4 — this repo has not adopted v4's CSS-first config), `cssVariables: true`, and aliases that resolve into `packages/ui/src` (`components`, `ui`, `utils`, `lib`, `hooks` all under the `@lumen/ui/...` namespace). The CLI requires those aliases to resolve via `tsconfig.json`'s `paths` at generation time (`packages/ui/tsconfig.json` carries a `"@lumen/ui/*": ["./src/*"]` mapping for this) — generated files are then hand-edited to this repository's existing plain-relative-import convention (`../../lib/cn`, not an aliased import) before being committed, since no other file in `@lumen/ui` uses path aliases and none of this repo's bundlers (Vite/vitest) are configured to resolve them at runtime.

## 3.2 Token bridge

`packages/ui/src/styles/shadcn-lumen-bridge.css` maps shadcn's fixed compatibility-variable vocabulary (`--background`, `--primary`, `--ring`, `--radius`, ...) one-directionally onto real Lumen semantic variables — e.g. `--ring: var(--color-border-focus)`, `--radius: var(--radius-lg)`. It never defines a `[data-theme="dark"]` block of its own: every Lumen variable it points at already swaps value under that attribute (see `packages/tokens/scripts/build.mjs`), so the bridge inherits dark mode for free. Exported from the package as `@lumen/ui/shadcn.css` and imported once, package-wide (currently by Storybook's `tailwind.css`; a consuming product app imports it the same way, alongside `@lumen/tokens/css`).

`packages/ui/src/styles/shadcn-tailwind-preset.cjs` is a small Tailwind preset extending `colors` (background/foreground/card/popover/primary/secondary/muted/accent/destructive/border/input/ring) and `borderRadius` (lg/md/sm, calculated from the single `--radius` variable) so that shadcn-generated utility classes like `bg-popover` or `rounded-lg` resolve through the bridge instead of Tailwind's own built-in defaults. Required by **both** `packages/ui/tailwind.config.cjs` and `packages/storybook/tailwind.config.cjs` (Storybook compiles `@lumen/ui`'s source directly) — kept as one shared file specifically so the two configs cannot drift apart.

## 3.3 Color format

`@lumen/tokens`' semantic colors resolve to full hex values (e.g. `#695cff`), not bare HSL channel triples. Every bridge mapping and Tailwind color key therefore uses `var(--x)` directly — never shadcn's own default `hsl(var(--x))` wrapping, which would produce invalid CSS like `hsl(#695cff)`.

---

# 4. Rules for every adopted component

1. **Lumen tokens are the visual source of truth.** No adopted component may introduce a color, radius, shadow, spacing, or type-size value that isn't traceable to a `@lumen/tokens` variable via the bridge.
2. **shadcn is source code, not a parallel design system.** Its default theme, its own color palette, and its own spacing/radius scale are never committed — only its composition/behavior code, adapted.
3. **Never duplicate an existing Lumen component.** Check `packages/ui/src/{primitives,composite,layout}` before adopting anything shadcn also ships (e.g. shadcn's own `Dialog` is *used internally* by `Command` here, but Lumen's `Modal` remains the public dialog component for direct product use — they are not redundant public APIs).
4. **Generated source requires design, code, and accessibility review before merge** — the CLI's output is a starting point, never final. Expect to change icons, colors, radii, shadows, and focus-ring classes in every file it produces.
5. **Default shadcn theme values must never be committed.** `packages/ui/src/styles/shadcn-lumen-bridge.test.ts` enforces this by asserting the bridge file never contains shadcn's known default HSL values (e.g. `222.2 84% 4.9%`, `0 0% 100%`).
6. **Public consumers import only through `@lumen/ui`.** `packages/ui/src/index.ts` never re-exports anything from a component's `internal/` path directly — only from its public wrapper module.
7. **Dark mode follows Lumen's existing `[data-theme]` mechanism.** No adopted component may introduce Tailwind `dark:` variant classes or a second theme-switching mechanism — this repo has none today, and every color already resolves correctly per-theme through CSS variables.
8. **Visual utility classes must resolve to Lumen semantic tokens**, either through the bridge (colors, radius) or an explicit arbitrary-value class pointing at a real token (e.g. `shadow-[var(--shadow-menu-default)]` for elevation, since shadcn has no shadow-token bridging convention of its own).
9. **Every adopted component ships Storybook coverage** — at minimum Default, a selected/highlighted state, a disabled state, an empty state, long/overflowing content, and an open-state (dialog/overlay) story where applicable. Dark mode is covered by the existing global theme-toolbar toggle, not a per-component duplicate story.
10. **Updating a component through the shadcn CLI is a source-code migration, not an automatic upgrade.** Re-running `pnpm dlx shadcn@latest add <name> --overwrite` regenerates the *unadapted* file; every adaptation in this document must be re-applied and re-reviewed, exactly like the first adoption.
11. **The Storybook `title` category is `Primitives` or `Composite` per `docs/component-architecture.md` §2.2/§2.3's own explicit example lists — never defaulted to whatever the previous component used, and never inferred without checking both lists in full.** §2.2 names `Divider` (Separator), `Spinner`, `Progress`, and `Skeleton` as primitives. §2.3 names `Menu`, `Dropdown`, `Accordion`, `Toast`, `Alert`, `Card`, `Dialog`, `Drawer`, `Popover`, and `Table` as composite — **`Alert` is composite, not primitive**, despite reading as a simple one-off component; don't assume a component belongs in Primitives just because it looks atomic without checking whether §2.3 already names it. For anything not explicitly named in either list (e.g. `AspectRatio`, `Kbd`, `HoverCard`, `ScrollArea`, `Slider`), fall back to the general definition: atomic/single-concept with no sub-composition (Primitives) vs. combines primitives into one interaction unit, especially anything with its own floating/positioned panel (Composite). Batch 1 initially filed all six of its components under `Composite/X` by copying `Command`'s title, then over-corrected by moving `Alert` to `Primitives/X` too without checking §2.3's own list — both mistakes caught and corrected the same day (see `docs/changelog.md`). This does not move the component's file location: `packages/ui/src/components/{internal,<name>}` stays the provenance/governance structure regardless of which Storybook category it's filed under.

---

# 5. Known gaps and judgment calls

- **No general-purpose "secondary/muted/accent" surface tokens exist** outside component-specific groups (Button's own `secondary-bg`, Badge's own tints). The bridge reuses the closest existing named token per role (`--secondary` → Button's secondary tokens, `--accent` → `background.nav-active`, the existing "this row is selected" surface) rather than inventing a new tier. Treat `--accent` in particular as a placeholder pending a purpose-built "selected row" token if design defines one.
- **No Lumen "scrim"/overlay-opacity token exists.** `Command`'s dialog overlay matches `Modal.tsx`'s own existing `bg-black/40` value rather than introducing a second one-off opacity.
- **No Lumen motion/duration tokens exist yet.** shadcn's default open/close animation utilities (`animate-in`, `fade-in-0`, `zoom-in-95`, ...) require the `tailwindcss-animate` plugin and were deliberately **not** added — open/close is instant for now. Add real motion once Lumen defines a duration/easing scale, rather than pulling in an unbacked animation vocabulary.
- **Icons:** `components.json` declares `iconLibrary: "lucide"` because the shadcn CLI requires *some* value, but no `lucide-react` import should ever survive adaptation — Lumen already has a generated icon set (`packages/ui/src/icons/generated`) and every icon a generated component needs so far (`search`, `x`/close, `check`) already exists there.
- **Tailwind opacity modifiers (`bg-primary/20`, `border-destructive/50`, ...) are not reliable against bridged colors.** shadcn's own default theme defines colors as bare HSL channel triples (e.g. `222.2 84% 4.9%`) specifically so `hsl(var(--x) / <alpha-value>)` can apply an opacity modifier. This repo's bridge points shadcn's variables at Lumen tokens that resolve to full hex values instead (see §3.3) — replace every `/NN` opacity modifier on a bridged color with a solid, already-appropriate token instead of assuming it degrades gracefully (`Alert`'s `border-destructive/50`, `Skeleton`/`Progress`'s `bg-primary/20`/`/10` were all fixed this way in batch 1).
- **Don't trust shadcn's generated source to be bug-free.** `Progress`'s adaptation caught a real accessibility bug in the upstream template: it destructures `value` out of props for the indicator's inline-style calculation but never forwards it back to `ProgressPrimitive.Root`, so Radix never received a value and never set `aria-valuenow`. `Slider`'s adaptation (batch 2) caught a second one: the generated source hardcodes a single `<SliderPrimitive.Thumb>`, so a range slider (`value`/`defaultValue` with more than one number) silently only gets one draggable handle even though Radix's own primitive supports one thumb per value — fixed by mapping one `Thumb` per value instead. Rule 4 ("generated source requires review, never blind acceptance") exists for exactly this kind of thing — verify behavior with a real test, not just a visual check. Both bugs were only caught because a test exercised the actual behavior (ARIA attributes, thumb count), not just a rendered-without-crashing smoke check — write tests that assert on real behavior for every new component, not just presence.

---

# 6. Adopted components

| Component | Status | Internal source | Public export |
|---|---|---|---|
| `Command` | Adopted 2026-07-23 | `packages/ui/src/components/internal/{command,dialog}.tsx` | `packages/ui/src/components/command/Command.tsx` |
| `Accordion` | Adopted 2026-07-23 | `packages/ui/src/components/internal/accordion.tsx` | `packages/ui/src/components/accordion/Accordion.tsx` |
| `Alert` | Adopted 2026-07-23 (batch 1) | `packages/ui/src/components/internal/alert.tsx` | `packages/ui/src/components/alert/Alert.tsx` |
| `Separator` | Adopted 2026-07-23 (batch 1) | `packages/ui/src/components/internal/separator.tsx` | `packages/ui/src/components/separator/Separator.tsx` |
| `Skeleton` | Adopted 2026-07-23 (batch 1) | `packages/ui/src/components/internal/skeleton.tsx` | `packages/ui/src/components/skeleton/Skeleton.tsx` |
| `Progress` | Adopted 2026-07-23 (batch 1) | `packages/ui/src/components/internal/progress.tsx` | `packages/ui/src/components/progress/Progress.tsx` |
| `AspectRatio` | Adopted 2026-07-23 (batch 1) | `packages/ui/src/components/internal/aspect-ratio.tsx` | `packages/ui/src/components/aspect-ratio/AspectRatio.tsx` |
| `Kbd` | Adopted 2026-07-23 (batch 1) | `packages/ui/src/components/internal/kbd.tsx` | `packages/ui/src/components/kbd/Kbd.tsx` |
| `Popover` | Adopted 2026-07-23 (batch 2) | `packages/ui/src/components/internal/popover.tsx` | `packages/ui/src/components/popover/Popover.tsx` |
| `DropdownMenu` | Adopted 2026-07-23 (batch 2) | `packages/ui/src/components/internal/dropdown-menu.tsx` | `packages/ui/src/components/dropdown-menu/DropdownMenu.tsx` |
| `Sheet` | Adopted 2026-07-23 (batch 2) | `packages/ui/src/components/internal/sheet.tsx` | `packages/ui/src/components/sheet/Sheet.tsx` |
| `ScrollArea` | Adopted 2026-07-23 (batch 2) | `packages/ui/src/components/internal/scroll-area.tsx` | `packages/ui/src/components/scroll-area/ScrollArea.tsx` |
| `HoverCard` | Adopted 2026-07-23 (batch 2) | `packages/ui/src/components/internal/hover-card.tsx` | `packages/ui/src/components/hover-card/HoverCard.tsx` |
| `Slider` | Adopted 2026-07-23 (batch 2) | `packages/ui/src/components/internal/slider.tsx` | `packages/ui/src/components/slider/Slider.tsx` |
| `Textarea` | Adopted 2026-07-23 (batch 3) | `packages/ui/src/components/internal/textarea.tsx` | `packages/ui/src/components/textarea/Textarea.tsx` |
| `Toggle` | Adopted 2026-07-23 (batch 3) | `packages/ui/src/components/internal/toggle.tsx` | `packages/ui/src/components/toggle/Toggle.tsx` |
| `InputOTP` | Adopted 2026-07-23 (batch 3) | `packages/ui/src/components/internal/input-otp.tsx` | `packages/ui/src/components/input-otp/InputOTP.tsx` |
| `ContextMenu` | Adopted 2026-07-23 (batch 3) | `packages/ui/src/components/internal/context-menu.tsx` | `packages/ui/src/components/context-menu/ContextMenu.tsx` |
| `Breadcrumb` | Adopted 2026-07-23 (batch 3) | `packages/ui/src/components/internal/breadcrumb.tsx` | `packages/ui/src/components/breadcrumb/Breadcrumb.tsx` |
| `Drawer` | Adopted 2026-07-23 (batch 3) | `packages/ui/src/components/internal/drawer.tsx` | `packages/ui/src/components/drawer/Drawer.tsx` |
| `Carousel` | Adopted 2026-07-23 (batch 3) | `packages/ui/src/components/internal/carousel.tsx` | `packages/ui/src/components/carousel/Carousel.tsx` |
| `Item` | Adopted 2026-07-23 (batch 3) | `packages/ui/src/components/internal/item.tsx` | `packages/ui/src/components/item/Item.tsx` |
| `Collapsible` | Adopted 2026-07-23 (batch 4) | `packages/ui/src/components/internal/collapsible.tsx` | `packages/ui/src/components/collapsible/Collapsible.tsx` |
| `Label` | Adopted 2026-07-23 (batch 4) | `packages/ui/src/components/internal/label.tsx` | `packages/ui/src/components/label/Label.tsx` |
| `ToggleGroup` | Adopted 2026-07-23 (batch 4) | `packages/ui/src/components/internal/toggle-group.tsx` | `packages/ui/src/components/toggle-group/ToggleGroup.tsx` |
| `NavigationMenu` | Adopted 2026-07-23 (batch 4) | `packages/ui/src/components/internal/navigation-menu.tsx` | `packages/ui/src/components/navigation-menu/NavigationMenu.tsx` |
| `Form` family | Adopted 2026-07-23 (batch 4) as fully `Shadcn`-prefixed; **partially promoted 2026-07-24** (§7.8) — `Form`/`FormItem`/`FormLabel`/`FormControl`/`FormDescription`/`FormMessage`/`useFormField` dropped the prefix, only `ShadcnFormField` kept it (the one real collision with Lumen's `FormField`) | `packages/ui/src/components/internal/form.tsx` | `packages/ui/src/components/form/Form.tsx` |
| `Button` | Adopted 2026-07-23 (batch 5) as `ShadcnButton`; **promoted to plain `Button` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/button.tsx` | `packages/ui/src/components/button/Button.tsx` |
| `Card` | Adopted 2026-07-23 (batch 5) as `ShadcnCard`; **promoted to plain `Card` 2026-07-24** (§7.8) after Lumen's original composite was retired | `packages/ui/src/components/internal/card.tsx` | `packages/ui/src/components/card/Card.tsx` |
| `Tabs` | Adopted 2026-07-23 (batch 5) as `ShadcnTabs`; **promoted to plain `Tabs` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/tabs.tsx` | `packages/ui/src/components/tabs/Tabs.tsx` |
| `Tooltip` | Adopted 2026-07-23 (batch 5) as `ShadcnTooltip`; **promoted to plain `Tooltip` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/tooltip.tsx` | `packages/ui/src/components/tooltip/Tooltip.tsx` |
| `Select` | Adopted 2026-07-23 (batch 5) as `ShadcnSelect`; **promoted to plain `Select` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/select.tsx` | `packages/ui/src/components/select/Select.tsx` |
| `Avatar` | Adopted 2026-07-23 (batch 5) as `ShadcnAvatar`; **promoted to plain `Avatar` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/avatar.tsx` | `packages/ui/src/components/avatar/Avatar.tsx` |
| `Input` | Adopted 2026-07-23 (batch 5) as `ShadcnInput`; **promoted to plain `Input` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/input.tsx` | `packages/ui/src/components/input/Input.tsx` |
| `Switch` | Adopted 2026-07-23 (batch 5) as `ShadcnSwitch`; **promoted to plain `Switch` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/switch.tsx` | `packages/ui/src/components/switch/Switch.tsx` |
| `Checkbox` | Adopted 2026-07-23 (batch 5) as `ShadcnCheckbox`; **promoted to plain `Checkbox` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/checkbox.tsx` | `packages/ui/src/components/checkbox/Checkbox.tsx` |
| `Pagination` | Adopted 2026-07-23 (batch 5) as `ShadcnPagination`; **promoted to plain `Pagination` 2026-07-24** (§7.8) after Lumen's original composite was retired — API shape changed from a single declarative component to composable parts | `packages/ui/src/components/internal/pagination.tsx` | `packages/ui/src/components/pagination/Pagination.tsx` |
| `ButtonGroup` | Adopted 2026-07-23 (batch 5) as `ShadcnButtonGroup`; **promoted to plain `ButtonGroup` 2026-07-24** (§7.8) after Lumen's original primitive was retired | `packages/ui/src/components/internal/button-group.tsx` | `packages/ui/src/components/button-group/ButtonGroup.tsx` |
| `Dialog` | Adopted 2026-07-23 (batch 5) | `packages/ui/src/components/internal/dialog.tsx` | `packages/ui/src/components/dialog/Dialog.tsx` |
| `RadioGroup` | Adopted 2026-07-23 (batch 5) | `packages/ui/src/components/internal/radio-group.tsx` | `packages/ui/src/components/radio-group/RadioGroup.tsx` |
| `Table` | Adopted 2026-07-23 (batch 5) | `packages/ui/src/components/internal/table.tsx` | `packages/ui/src/components/table/Table.tsx` |
| `Calendar` | Adopted 2026-07-24 (batch 6) | `packages/ui/src/components/internal/calendar.tsx` | `packages/ui/src/components/calendar/Calendar.tsx` |
| `Chart` | Adopted 2026-07-24 (batch 6) | `packages/ui/src/components/internal/chart.tsx` | `packages/ui/src/components/chart/Chart.tsx` |
| `Menubar` | Adopted 2026-07-24 (batch 7) | `packages/ui/src/components/internal/menubar.tsx` | `packages/ui/src/components/menubar/Menubar.tsx` |
| `Resizable` | Adopted 2026-07-24 (batch 7) | `packages/ui/src/components/internal/resizable.tsx` | `packages/ui/src/components/resizable/Resizable.tsx` |
| `InputGroup` | Adopted 2026-07-24 (batch 7) | `packages/ui/src/components/internal/input-group.tsx` | `packages/ui/src/components/input-group/InputGroup.tsx` |

`Accordion`'s adoption also surfaced that the shadcn CLI itself edits `tailwind.config.cjs` on generation — for this component it added `darkMode: ["class"]` and hardcoded `accordion-down`/`accordion-up` keyframes at a fixed `0.2s` duration. Both were reverted as part of adaptation: this repo has no Tailwind `dark:` usage anywhere (rule 7), and the hardcoded duration has no Lumen motion token backing it (see "Known gaps" above) — expand/collapse is instant, the same deferred-motion decision already made for Command's dialog. Check `tailwind.config.cjs` after every future `shadcn add` invocation for the same kind of unreviewed edit.

## 7.4 Batch 3 notes

- **`Marker`, `Direction`, and `Attachment` were requested but dropped from batch 3**: none of the three resolve at `https://ui.shadcn.com/r/styles/new-york/<name>.json` — the registry's top-level index lists them, but they aren't actually published for the `new-york` style this repo's `components.json` uses. Not a judgment call, a hard CLI failure (`The item ... was not found`). Re-check availability before a future attempt.
- **Drawer's real dependency turned out to be `vaul`, not `@base-ui/react`.** The registry's static metadata (fetched via `r/index.json`) listed `@base-ui/react` for both `drawer` and `direction`, which is what motivated the earlier "adopt both, add Base UI" decision — but actually running the CLI for `new-york`-style `drawer` revealed it depends on `vaul` instead (the classic shadcn drawer library, unrelated to Base UI). `@base-ui/react` was installed and remains in `package.json` but **nothing in this repo actually imports it** — it was added in anticipation of `direction`, which then turned out to be unavailable for this style (see above). Don't trust registry metadata over what the CLI actually generates; re-verify before removing or relying on either dependency in a future batch.
- **Carousel (`embla-carousel-react`) needed three jsdom environment stubs** neither this repo nor any prior batch required: `window.matchMedia` (also needed by `vaul`/Drawer), `document.elementFromPoint` (needed by `input-otp`/InputOTP), and `IntersectionObserver`. All three were added to `packages/ui/vitest.setup.ts` as no-op stubs, same pattern as the existing `ResizeObserver` stub. Expect any future component built on a real layout/gesture library (not just Radix) to need similar stubs — check the test failure's stack trace for which jsdom-missing API it needs before assuming the component itself is broken.
- **A genuine, unanticipated naming collision**: `PageHeader.tsx` already exported an interface named `Breadcrumb` (a `{label, href}` shape for its own `breadcrumbs` prop), colliding with the new `Breadcrumb` *component*. Unlike the Button/Card/Tabs-style collisions §7.1 anticipated (a full public component duplicating another full public component), this was a small internal data-shape type colliding by accident. Resolved by renaming the type to `PageHeaderBreadcrumb` (updating its one real consumer, `packages/patterns/src/DashboardPage.tsx`) rather than prefixing the new component — keeps the more valuable, more-used name (`Breadcrumb`) on the actual component. §7.1's `Shadcn`-prefix convention is specifically for same-purpose public-component collisions; a small internal type is a rename candidate instead. Check for this kind of accidental collision (not just the ten components named in §7.1) on every future batch.
- **Carousel's Previous/Next controls use shadcn's own internal `Button`** (`packages/ui/src/components/internal/button.tsx`, generated as Carousel's dependency), not Lumen's public `Button` — Lumen's `Button` has no icon-only circular variant suited to this exact pattern, matching rule 3's existing precedent (`Command` uses its own internal `Dialog` rather than Lumen's `Modal`). This internal `Button` is not exported publicly; the `Shadcn`-prefixed public naming decision from §7.1 for Button/Card/Tabs/etc. is still deferred to its own batch.

## 7.5 Batch 4 notes

- **`Combobox` and `NativeSelect` were requested but dropped**: same failure mode as `Marker`/`Direction`/`Attachment` in batch 3 — neither resolves at `https://ui.shadcn.com/r/styles/new-york/<name>.json` for this repo's `new-york` style. Not a judgment call; re-check availability before a future attempt.
- **`Sidebar` and `Message`/`MessageScroller`/`Bubble` were requested but skipped by direct user decision**, not adopted even in `Shadcn`-prefixed form. `Sidebar` is shadcn's heaviest component — its own provider, mobile-sheet variant, keyboard shortcuts, and persisted collapse state — functionally a complete competing app-shell/navigation system, not an overlapping primitive; `AppShell` already fills that role for this design system, Figma-sourced and actively maintained. `Message`/`MessageScroller`/`Bubble` are shadcn's AI-chat components, direct conceptual competitors to `AIPanel` (also Figma-sourced). Unlike every other overlap in this bulk effort, these were judged *not* worth adopting even as a separate, clearly-labeled alternative — revisit only if a real gap in `AppShell` or `AIPanel` surfaces that they'd genuinely fill.
- **`Form` is the first component in this effort adopted specifically *because* of its overlap** (with Lumen's own `FormField`), by direct user decision, rather than despite it. Brings in `react-hook-form`, `@hookform/resolvers`, and `zod` — the first form-state-management library in `@lumen/ui` (distinct in kind from every other dependency added so far, all either Radix primitives or single-purpose behavioral libraries like `vaul`/`embla-carousel-react`). Exported as `ShadcnForm`/`ShadcnFormField`/`ShadcnFormItem`/`ShadcnFormLabel`/`ShadcnFormControl`/`ShadcnFormDescription`/`ShadcnFormMessage`/`useShadcnFormField` — the *entire family* prefixed, not just the one symbol (`FormField`) that collides by name, so the two systems read as clearly distinct rather than colliding piecemeal. Field visuals still come from Lumen's own `Input`/`Button` inside the `ShadcnFormControl` slot — `react-hook-form` only supplies state/validation, never a competing visual layer.
- **`toggle.tsx` and `button.tsx` were silently reverted by the CLI a second time** (batch 3 already hit this once) — this time as registryDependencies of `toggle-group` and `form` respectively. Confirms this is a standing risk of `--overwrite`, not a one-off: **diff every already-adapted file after any `shadcn add` invocation**, not just the newly-requested ones.
- **Collapsible vs. Accordion, ToggleGroup vs. SegmentedControl, NavigationMenu vs. AppShell's nav**: all three adopted with a documented distinction (see each component's own internal-source header comment) rather than skipped, since the overlap is partial, not a full functional duplicate the way `Sidebar`/`Message` would have been.

## 7.6 Batch 5 notes

- **`ButtonGroup` was missed in the original batch-5 plan's "no collision" list** — it was reported as safe to keep its own plain name, but Lumen already has its own `ButtonGroup` primitive (`packages/ui/src/primitives/ButtonGroup.tsx`). Caught by re-verifying every planned name against the actual current `@lumen/ui` export surface (`grep` for exact `export const/function` matches) before touching anything, rather than trusting the prior batch's summary from memory — the same discipline the batch-3 `Breadcrumb` mistake should have already established. Corrected to `ShadcnButtonGroup`, so the final split is **11 `Shadcn`-prefixed** (`ShadcnButton`, `ShadcnCard`, `ShadcnTabs`, `ShadcnTooltip`, `ShadcnSelect`, `ShadcnAvatar`, `ShadcnInput`, `ShadcnSwitch`, `ShadcnCheckbox`, `ShadcnPagination`, `ShadcnButtonGroup`) and **3 plain-named** (`Dialog`, `RadioGroup`, `Table`).
- **`toggle.tsx`, `button.tsx`, and `separator.tsx` were silently reverted by the CLI yet again** — the third time for `separator.tsx`, fourth for `button.tsx` (this time as registryDependencies of `button-group`/`pagination` and `dialog`/`separator` respectively; `dialog.tsx` itself was skipped as "identical," so it survived untouched). This is now a fully established pattern across every batch since batch 3 — always diff every previously-adapted file after `shadcn add`, never assume only the newly-named components changed.
- **Two new bare-utility patterns established**, extending the existing "opacity modifiers are unreliable" and "menu/popover shadow → `--shadow-menu-default`" rules from earlier batches:
  - Bare `shadow`/`shadow-sm`/`shadow-xs` (no Lumen precedent on the equivalent hand-built component — Button, Switch, Checkbox, RadioGroup, ButtonGroup all ship with no box-shadow) are simply dropped, not replaced with a token.
  - Bare `shadow` specifically on **resting, non-overlay surfaces** (`ShadcnCard`, `ShadcnTabs`'s active-tab state) resolves to the previously-unused `--shadow-elevation-sm` token — a distinct tier from `--shadow-menu-default`, which stays reserved for floating overlays (Popover/Select/Command/menus).
- **`ShadcnTooltip`'s default shadcn styling (`bg-primary`/brand-colored) was replaced with Lumen's own existing dark-neutral-inverse tooltip look** (`--color-background-inverse`/`--color-text-inverse`), matching Lumen's hand-built `Tooltip.tsx` (`bg-neutral-800`/`text-neutral-white`) exactly but sourced through real semantic tokens instead of raw neutral literals — the one component in this batch where shadcn's own default color choice was overridden for cross-system visual consistency, not just token-correctness.
- **`ShadcnForm`'s `Meta` typing precedent (dropping the `component:` binding when a component's prop type can't be faked with placeholder `args`) did not recur here** — every batch-5 component's props were concrete enough for normal `Meta<typeof X>` typing or the same `args: { type: "..." }` placeholder pattern already used for `Accordion`/`InputOTP`/`ToggleGroup`.

This closes the "name-colliding duplicates" phase of the bulk-adoption plan (§7). Remaining: `Calendar`/`Chart` (heaviest dependencies — a date library, Recharts), and the `Combobox`/`NativeSelect`/`Marker`/`Direction`/`Attachment` gaps that were dropped as unavailable for the `new-york` registry style across batches 3–5 (re-check availability before attempting again).

## 7.7 Batch 6 notes

- **`Calendar` and `Chart` were each flagged as their own bigger decision before adoption** (new dependency ecosystems, not just another Radix primitive) — `react-day-picker` + `date-fns` for `Calendar` (this repo's first date-handling libraries), `recharts` for `Chart` (this repo's first charting library, and the first component whose visual identity is a *categorical color palette* rather than a themeable neutral surface). Both approved directly by the user; `Chart` specifically approved with an explicit instruction to apply the `dataviz` skill's palette/style rules rather than shadcn's default CSS-variable-per-series scheme, since Lumen has no chart color tokens of its own and `docs/component-architecture.md` §2.4 lists an unbuilt "Data Visualization Container."
- **No naming collisions for either component** — re-verified against the actual current `@lumen/ui` export surface (`grep` for exact `export const/function/interface/type` matches across `packages/ui/src`, not memory), per the discipline established after the batch-3 `Breadcrumb` and batch-5 `ButtonGroup` misses. Both `Calendar` and `Chart` keep their own plain shadcn names, including the `Chart` sub-parts (`ChartTooltip`, `ChartTooltipContent`, `ChartLegend`, `ChartLegendContent`, `ChartStyle`) — none collide with Lumen's generated `Chart*Icon` set, which are differently-named icon components, not the chart primitives themselves.
- **Registry metadata was wrong again, the same failure mode already seen with `drawer`/`vaul` in batch 3**: the static registry listed `recharts@3.8.0` for `chart`, but the CLI's own generation step explicitly requested `recharts@2.15.4` in its install command. `recharts@^3.10.0` was installed first (matching the registry), then downgraded to the exact `2.15.4` the generated component code expects (accepting an npm deprecation warning on that version — it's the last 2.x release). Trust what the CLI actually does over static registry JSON, as already documented in §7.4.
- **The corepack pnpm version-drift problem (recurring since early batches) was finally worked around at the root** instead of pre-installing around it each time: a direct, non-corepack `pnpm` binary was found already pinned to the exact repo-required version (`11.11.0`, at `/c/Users/sriku/AppData/Roaming/npm/pnpm`), and invoked directly (`pnpm dlx shadcn@latest add calendar chart --yes --overwrite`) instead of routing through `corepack pnpm dlx`, which kept resolving a drifted `11.12.0` and failing the CLI's internal install step. Prefer this direct-binary invocation over the pre-install workaround in any future batch, if the binary is still present and still pinned correctly.
- **`button.tsx` and `card.tsx` were silently reverted by the CLI yet again** — fifth revert for `button.tsx`, second for `card.tsx` — as registryDependencies of `calendar` (which uses the internal `Button`) and, in card's case, general CLI churn on re-generation. Both re-fixed with their established adaptations. This is now confirmed across six consecutive batches: **always diff every previously-adapted file after any `shadcn add` invocation**, not just the newly-requested component.
- **`calendar.tsx` adaptation**: relative imports; lucide's `ChevronDownIcon`/`ChevronLeftIcon`/`ChevronRightIcon` replaced with Lumen's own generated icons of the same names (`packages/ui/src/icons/generated`); `text-sm`/`text-[0.8rem]`/`text-xs` replaced with Lumen's `label-md`/`label-sm` type scale; bare `shadow-xs` on the month/year dropdown trigger dropped (no Lumen precedent, consistent with the batch-5 bare-shadow rule); `ring-ring/50` replaced with the solid bridged `ring-ring` (opacity modifiers are unreliable against this repo's raw-hex bridged colors, per §5).
- **`chart.tsx` adaptation**: relative imports; `text-xs` replaced with `label-sm`; `border-border/50` and `stroke-border/50` opacity modifiers replaced with solid `border-border`/`stroke-border`; the tooltip's bare `shadow-xl` replaced with the bridged `--shadow-menu-default` (a hover tooltip is a floating overlay, the same tier used for Popover/Select/Command); and — the one adaptation unique to this component — the `THEMES` constant's dark-mode selector was rewritten from Tailwind's `.dark`-class convention to this repo's actual `[data-theme="dark"]` attribute selector, since this repo has zero Tailwind `dark:`/`.dark` usage anywhere else (rule 7). Without this fix, every series' dark-mode color would have silently never applied.
- **The dataviz skill's categorical palette was computed, not eyeballed**, per the skill's own non-negotiable. Starting from real hex values in `packages/tokens/src/primitives/color.json` and deliberately avoiding green/orange/red (reserved for status semantics, not identity), a 6-step palette was iterated through the skill's `validate_palette.js` against both the light and dark chart surface separately:
  - **Light** (surface: card background): `#BE003C, #0057D8, #998200, #AA00FF, #006D99, #990C58`
  - **Dark** (surface: dark-mode card background): `#CB3363, #3379E0, #998200, #BB33FF, #0091CC, #CC1076`
  - All adjacent pairs cleared the CVD ΔE and normal-vision floors in both modes except the 6th step (a magenta/pink) against the dark surface, which the validator returned a contrast WARN for — per the skill's rule, this is not dismissable, so `Chart.stories.tsx`'s `CategoricalPalette` story gives that series' bars a permanent direct value label (`recharts`' `LabelList`) rather than relying on the legend swatch alone.
  - Exposed to consumers as `chartCategoricalColors` (`packages/ui/src/components/chart/Chart.tsx`), an ordered array of `{ light, dark }` pairs meant to be assigned to `ChartConfig` entries via the `theme` field in fixed index order — never cycled or reassigned by rank, per the skill's non-negotiables. A 7th+ series should fold into "Other," small multiples, or composite encoding rather than extending this array.
- **Storybook category**: neither `Calendar` nor `Chart` is named in `docs/component-architecture.md` §2.2 or §2.3's explicit example lists (checked in full per rule 11) — §2.3 names the closely related `Date Picker`, and §2.4 names `Data Visualization Container`, but no `Enterprise` Storybook category exists in this repo's actual taxonomy (only `Primitives`/`Composite`/`Layout`/`Patterns`/`AI Components` are in use — verified by grepping every `title:` in `packages/ui/src` and `packages/patterns/src`). Both components were filed under `Composite/` on rule 11's fallback definition: each combines an internal `Button` (Calendar) or a Recharts plot plus floating tooltip/legend panel (Chart) into one interaction unit, rather than standing alone as an atomic control.
- **This closed out every component from the original ~61-item shadcn `registry:ui` set known at the time** across 6 batches, combined with the `Combobox`/`NativeSelect`/`Marker`/`Direction`/`Attachment` gaps (unavailable for this repo's `new-york` registry style) and the two components skipped by direct user decision (`Sidebar`; `Message`/`MessageScroller`/`Bubble` — see §7.5). See §7.9 for batch 7, opened 2026-07-24 to cover shadcn registry components that didn't exist (or weren't requested) at the time of batches 1–6.

## 7.9 Batch 7 notes

Requested 2026-07-24 as a fresh, explicit bulk request (`Bubble, Attachment, Checkbox, Collapsible, Combobox, Command, Data Table, Date Picker, Direction, Empty, Field, Input OTP, Input Group, Kbd, Label, Marker, Menubar, Message, Message Scroller, Native Select, Navigation Menu, Popover, Progress, Radio Group, Resizable, Scroll Area, Select, Toggle` — 28 names), with an explicit second clause to audit every one already adopted for shadcn functionality/behavior/interaction/API parity (see §7.10). Of the 28:

- **13 already existed** from earlier batches (`Checkbox`, `Collapsible`, `Command`, `Input OTP`, `Kbd`, `Label`, `Navigation Menu`, `Popover`, `Progress`, `Radio Group`, `Scroll Area`, `Select`, `Toggle`) — not rebuilt, only in scope for the §7.10 audit.
- **10 don't resolve as installable shadcn components**, re-verified directly against both `https://ui.shadcn.com/r/<name>.json` and `https://ui.shadcn.com/r/styles/new-york/<name>.json` (both 404) rather than trusting the registry's top-level name index (which lists all 10 anyway — the same "index lists it, nothing is actually published" failure mode as `Marker`/`Direction`/`Attachment` in batch 3 and `Combobox`/`NativeSelect` in batch 4): **`Bubble`, `Attachment`, `Combobox`, `Data Table`, `Date Picker`, `Direction`, `Marker`, `Message`, `Message Scroller`, `Native Select`**. `Bubble`/`Message`/`Message Scroller` were also already judged in batch 4 (§7.5) as deliberate, non-availability-related skips (AIPanel-competing AI-chat components) — both reasons now independently apply.
- **5 were genuinely new and adopted this batch**: `Menubar`, `Resizable`, `Input Group` (below); **`Empty`** and **`Field`** were requested but skipped by direct user decision after being flagged as full functional duplicates of existing Lumen composites (`EmptyState` and `FormField` respectively) rather than name collisions — same category of call as `Sidebar`/`Message` in batch 4, re-litigated per-component via `AskUserQuestion` rather than defaulted.

**Standing per-batch risks recurred again**: `button.tsx`, `input.tsx`, and `textarea.tsx` were silently reverted by the CLI as registryDependencies of `input-group` (sixth, first, and first revert respectively for these three files) — re-fixed via `git checkout --` from the pre-CLI committed state rather than re-typing the adaptations by hand, since nothing had been committed in between.

**`menubar.tsx` adaptation**: structurally near-identical to `dropdown-menu.tsx`/`context-menu.tsx` (all Radix menu primitives) — same adaptation recipe applied: relative imports, `CheckIcon`/`ChevronRightIcon`/`DotIcon` instead of lucide, `body-sm`/`label-sm` instead of bare `text-sm`/`text-xs`, `--shadow-menu-default` on floating content instead of `shadow-lg`/`shadow-md`, dropped `animate-in`/etc. The root's own bare `shadow-sm` (a resting, non-overlay toolbar surface) was mapped to `--shadow-elevation-sm` — the same tier established for `Card`/`Tabs`' resting surfaces in batch 5, not the floating-overlay tier.

**`input-group.tsx` adaptation and two real bugs caught**:
- Standard fixes: relative imports, dropped every `dark:` class (dark mode is `[data-theme]`-driven; `--destructive` already resolves through a theme-aware token so the `dark:ring-destructive/40` variant was simply unnecessary, not just unreliable), the aria-invalid error ring's opacity modifiers replaced with solid `ring-destructive`/`border-destructive`, root's bare `shadow-xs` dropped, addon/text content moved to `input-md` to match `Input`'s own scale.
- A real, non-cosmetic bug: `InputGroupButton`'s generated base classes included a bare `text-sm` that would have silently overridden `Button`'s own `text-label-md`/`text-label-sm` — `cn()`'s tailwind-merge extension (see `packages/ui/src/lib/cn.ts`) treats typography-scale classes and bare `text-*` font-size classes as the same conflict group, and the later class in the merge order wins. Fixed by dropping the redundant `text-sm` entirely, letting `Button`'s own type token pass through unmodified.
- A real accessibility trade-off, not a token issue: the generated `InputGroupAddon` includes a `role="group"` div with an `onClick` that focuses a sibling input (click anywhere in the addon's padding to focus the adjacent control). Every combination tried — `role="group"` + click, plain div + click, `role="group"` + click + a matching `onKeyDown` — still failed one of this repo's `jsx-a11y/no-noninteractive-element-interactions` / `no-static-element-interactions` / `click-events-have-key-events` rules, because fully satisfying all three requires a real `tabIndex`, which would add a confusing, purposeless extra Tab stop before every input in the app (including purely decorative icon-only addons). Rather than suppress the lint (no `eslint-disable` exists anywhere in `packages/ui/src/components/internal` — every prior batch fixed real findings instead), the convenience behavior was dropped entirely: `InputGroupInput`/`InputGroupButton`/etc. remain independently and correctly focusable/clickable, so no essential functionality is lost, consistent with §7.8's "accept a minor API/behavior gap rather than paper over it" precedent.

**`resizable.tsx` adaptation and two real upstream-version bugs caught** — both stemming from the same root cause: the installed `react-resizable-panels@4.12.2` (what the CLI's own install step requested) has a materially newer, incompatible API than what shadcn's generated template assumes, verified directly against the package's own bundled `.d.ts` and compiled source rather than assumed from familiarity with the library's classic API:
1. **No `PanelGroup`/`Panel`/`PanelResizeHandle` exports exist at all** in this version — renamed to `Group`/`Panel`/`Separator` (`Panel` alone unchanged). The generated source fails to typecheck outright, not just to style incorrectly. Fixed by importing the real names and re-exporting under shadcn's documented public names (`ResizablePanelGroup`/`ResizablePanel`/`ResizableHandle`), so the public API surface still matches what shadcn documents even though the internal library names moved.
2. **`Group`'s orientation prop is `orientation`, not `direction`, and `Panel`'s `defaultSize`/`minSize`/`maxSize` now interpret a bare number as pixels** (a string without units is percent) — a silent behavior change from the classic API this component's own prop contract (and every other shadcn doc example) assumes. Fixed by keeping the public prop names/semantics shadcn documents (`direction`, plain percentage numbers for size props) and translating at the wrapper boundary (`toPercent()`), rather than leaking the installed library's current internal naming into the public API.
3. **A third, narrower bug**: the generated source styles `ResizableHandle`'s vertical/horizontal visual swap (thin bar width/height, grip rotation) via a `data-[panel-group-direction=vertical]:*` Tailwind selector, but this library version never sets that attribute anywhere (confirmed against its compiled source — only `data-group`/`data-panel`/`data-separator`/`data-disabled` exist, plus `flexDirection` as an inline style on the group). The group's own layout is unaffected (the inline style already sets the correct flex direction), but the handle's own visual swap silently never triggered — a real, not cosmetic, bug, caught by writing a regression test rather than a visual-only check (per §5's standing rule: "verify behavior with a real test, not just a visual check"). Fixed by keying off the handle's own `aria-orientation` attribute instead (which the library does set correctly and unconditionally), via Tailwind's `aria-[orientation=...]` arbitrary variant.

`GripVerticalIcon` already existed in Lumen's generated icon set, matching lucide's `GripVertical` exactly — no new icon needed. Dropped the Next.js `"use client"` directive (no other file in this repo uses it).

**Storybook category**: `Menubar` is explicitly named (`Menu`) in `docs/component-architecture.md` §2.3 → `Composite/Menubar`. `Resizable` and `InputGroup` aren't named in either §2.2 or §2.3's example lists (checked in full per rule 11) — both filed under `Composite/` on rule 11's fallback definition (each combines multiple primitives into one interaction unit: `InputGroup` wraps `Input`/`Textarea`/`Button` with addons, matching §2.3's named "Search Field"/"Password Field" precedent; `Resizable` combines a panel group, panels, and a draggable handle).

**Validation**: repo-wide `pnpm -r typecheck`, `pnpm --filter @lumen/ui test` (304 tests, including 6 new `Menubar`, 7 new `InputGroup`, and 6 new `Resizable` tests), repo-wide `pnpm lint`, and `pnpm --filter @lumen/storybook build-storybook` all passed.

## 7.10 Batch 7 parity audit (existing components)

See the corresponding `docs/changelog.md` `[Unreleased]` entries for the per-component findings and fixes from auditing the 13 already-adopted components (`Checkbox`, `Collapsible`, `Command`, `Input OTP`, `Kbd`, `Label`, `Navigation Menu`, `Popover`, `Progress`, `Radio Group`, `Scroll Area`, `Select`, `Toggle`) against shadcn's live registry source for structural/API and behavioral (keyboard, focus, ARIA) parity.

## 7.8 Reversal: the 11 name-colliding duplicates were un-duplicated (2026-07-24)

By direct user instruction on 2026-07-24, the `Shadcn`-prefixed duplication convention from §7.1 was reversed for all 11 components that had one: **`Button`, `Card`, `Tabs`, `Tooltip`, `Select`, `Avatar`, `Input`, `Switch`, `Checkbox`, `Pagination`, `ButtonGroup`.** For each, Lumen's original hand-built component was deleted outright and the `Shadcn`-prefixed shadcn-sourced version was promoted to the plain name (moved from `packages/ui/src/components/shadcn-<name>/Shadcn<Name>.tsx` to `packages/ui/src/components/<name>/<Name>.tsx`, every `Shadcn<Name>` identifier renamed to `<Name>`). This is the opposite of the additive, non-breaking stance §7.1 originally took — it was explicitly re-litigated and approved, not a default policy for any future collision.

**Why:** the user wanted a single canonical `Button`/`Card`/etc. (the shadcn-sourced look) rather than two parallel same-purpose components living side by side under different names.

**Scope-discovery process:** the real footprint was audited *before* touching anything, using two rounds of `AskUserQuestion` once genuine surprises surfaced — this is the pattern to repeat for any future "swap the canonical implementation" request, not just this one:
1. First pass scoped the "obvious" consumers (`AuthForm`, `CrudListPage`, `SettingsPage`, `Pagination.tsx`, `AIPanel.tsx`) and flagged that `Pagination`'s replacement wasn't a rename at all — shadcn's `Pagination` is a set of composable parts (`PaginationContent`/`PaginationItem`/`PaginationPrevious`/`PaginationNext`), not a single `page`/`pageCount`/`onPageChange` component, and its Previous/Next controls render `<a>` anchors (no native `disabled`) where the original used real `<button disabled>` elements.
2. A second, deeper pass (grepping every `from ".../primitives/X"` and `from "@lumen/ui"` barrel import across `packages/ui`, `packages/patterns`, and `packages/create-app` — not just the files that seemed obviously relevant) found the true footprint was ~30 files, not 5: nearly every other component's Storybook demo story renders a `Button`/`Input` alongside itself, and `packages/create-app`'s shipped React starter template (not a gitignored generated app — the template itself lives in this repo) also consumes `Button`/`Card`/`Input`. This was reported back before proceeding, together with two real API gaps neither prior pass had surfaced: `variant="accent"` (used by `AIPanel`'s real send button) and `iconStart`/`iconEnd` icon-slot props (used by several icon+label buttons) have no shadcn equivalent.
3. Even after both rounds of user confirmation, a **third, unplanned gap surfaced mid-migration**: `packages/patterns/src/SettingsPage.stories.tsx` used `Switch`'s retired `label` prop (barrel-imported via `"@lumen/ui"`, which neither of the first two grep passes were scoped to check for the six components assumed "zero-consumer") — caught only because `pnpm -r typecheck` failed after the swap. This is the same lesson as the batch-3 `Breadcrumb` and batch-5 `ButtonGroup` misses, generalized: **grep for barrel imports (`from "@lumen/ui"`) across every consuming package, not just relative imports within `packages/ui` itself** — a component can have zero relative-path consumers inside `packages/ui` and still have a real consumer elsewhere via the barrel.

**Decision on the two real API gaps (Button):** resolved by direct user instruction toward the strict/mechanical option in both cases, not by extending shadcn's Button to backfill Lumen-specific capability:
- `variant="accent"` (`AIPanel.tsx`'s send button) — the `accent` prop was dropped entirely (falls back to shadcn's `default` variant); no `accent` variant was added to the internal `button.tsx`. The user was explicit: *"I don't want accent variant."*
- `iconStart`/`iconEnd` — every call site (`PageHeader.stories.tsx`, `AppShell.stories.tsx` ×2, `DashboardPage.stories.tsx`) was rewritten to pass the icon as a plain JSX child instead (shadcn's Button base classes already size/space `<svg>` children correctly via `[&_svg]:size-4 [&_svg]:shrink-0`, so this is a pure JSX-shape change, not a visual regression).
- This same "accept the API loss, don't extend shadcn to compensate" philosophy was applied consistently to every other gap found: `Input`'s `size`/`variant="search"`/leading-icon/shortcut-badge props (dropped; `AIPanel.tsx` and `AppShell.stories.tsx`'s `SearchBar` now render a plain input, losing the search icon and ⌘K shortcut badge decoration), `Avatar`'s `name`/`tone`/`size` props (dropped; `AppShell.stories.tsx` now composes `AvatarFallback` with a literal `"JD"` instead of computing initials from a name), and `Switch`'s `label` prop (dropped; `SettingsPage.stories.tsx` now composes a separate `Label` next to each `Switch`).

**`Pagination`'s API-shape change**, per the user's explicit choice between the two options presented: `PaginationPrevious`/`PaginationNext` are rendered as `<a href="#">` anchors with `aria-disabled` + `tabIndex={-1}` + a `pointer-events-none opacity-50` className + an `onClick` handler that calls `e.preventDefault()` and only fires `onPageChange` when not at the boundary — see `packages/patterns/src/CrudListPage.tsx` for the concrete composition (`Pagination`/`PaginationContent`/`PaginationItem` wrapping a `<li>` with the "Page X of Y" text between the two controls, since `PaginationContent` renders a `<ul>` and only `<li>`-shaped children belong inside it). No wrapper wrapping `PaginationPrevious`/`PaginationNext` back into a single reusable `page`/`pageCount`/`onPageChange` component was created — consistent with every other component in this reversal, the composable shadcn shape is now the only public API; a future consumer needing the same Previous/Next pattern composes it inline the same way, rather than the design system re-introducing a bespoke convenience wrapper around shadcn's own primitives.

**`Card`'s padding model changed**: the retired primitive padded its own root (`p-6`) directly, so consumers put children straight inside `<Card>`. shadcn's `Card` root has *no* padding — it lives on `CardHeader`/`CardContent`/`CardFooter` — so every consumer with a loose (non-`CardHeader`) child needed that child wrapped in a new `<CardContent>`: `AuthForm.tsx` (the `<form>`), `CrudListPage.tsx` (the search `Input`), `SettingsPage.tsx` (`{s.content}`), `DashboardPage.stories.tsx`, and the `packages/create-app` template.

**`Tabs`' sub-component names changed** (root API unchanged — `value`/`defaultValue`/`onValueChange` already matched Radix's convention): `TabList`→`TabsList`, `Tab`→`TabsTrigger`, `TabPanel`→`TabsContent`. Only real consumer: `SettingsPage.tsx`.

**Consumers touched in total**: `packages/ui/src/composite/AIPanel.tsx` (+ its test), `packages/ui/src/layout/AppShell.stories.tsx`, `packages/ui/src/composite/{PageHeader,EmptyState,Modal,Toast}.stories.tsx`, every `components/*/{Collapsible,Command,Dialog,Drawer,DropdownMenu,Item,Popover,Sheet}.stories.tsx` that renders a demo `Button`, `components/shadcn-form/ShadcnForm.{stories,test}.tsx`, `components/button-group/ButtonGroup.{stories,test}.tsx` (fixing the `ShadcnButton` references deferred from the original promotion), `packages/patterns/src/{AuthForm,CrudListPage,SettingsPage,DashboardPage.stories,SettingsPage.stories}.tsx`, and `packages/create-app/templates/react-vite/src/App.tsx`. Full list and per-file rationale in the corresponding `git diff`; not repeated here since it will drift.

**Validation**: `pnpm --filter @lumen/ui typecheck`/`test` and repo-wide `pnpm -r typecheck`/`pnpm --filter @lumen/patterns test`/`pnpm lint` all passed after the fixes above (two real regressions caught this way: the `Icon.stories.tsx` search input's `onChange` handler lost its contextual `ChangeEvent` type inference once `Input`'s prop type resolved through the new component, needing an explicit annotation; and the `SettingsPage.stories.tsx` `Switch`/`label` gap in point 3 above).

**Immediate follow-up, same day:** once `Button` became the one canonical Button, the user asked whether its style tokens could be brought in line with Figma — see `docs/changelog.md`'s `[Unreleased]` §Fixed entries ("Synced the shadcn-sourced `Button` component's colors..." and the dark-mode follow-up directly after it) for the full record. Short version: the canonical Button component-set (node `1174:1349`) already had exact-matching Lumen primitives for `Primary`/`Ghost`/`Destructive`/`Disabled` — the gap was that `internal/button.tsx` was still wired to shadcn's generic bridge tokens instead of them (and had a real bug: `hover:bg-primary` etc. were no-ops). `Secondary` and `Outline`'s hover tokens had genuinely drifted from Figma's current values and were re-sourced via `get_variable_defs` — first for light mode, then a second time for dark mode after the user flagged the initial dark-mode render looked wrong. That second pass revealed the same node resolves dark values through Figma's variable *mode* system (not a separate `Theme=Dark` variant instance, which doesn't exist) — worth remembering for any future Figma sync on this file: a component with only `Theme=Light` variant instances can still have real, different, authoritative dark-mode color data, just reached by re-querying with the live Figma selection set to dark rather than by looking for a dark-named sibling node. `Accent`/`Link` remain unwired in both modes (no authored Figma states, consistent with this section's "no accent variant" decision above).

**Second follow-up, same day:** the user noticed the `ShadcnForm` family still showed a `Shadcn` prefix in Storybook and asked to fix it. Unlike the 11 components in the main reversal above, `Form`'s family has only *one* symbol that actually collides by name with an existing Lumen export (`FormField`) — the rest (`Form`, `FormItem`, `FormLabel`, `FormControl`, `FormDescription`, `FormMessage`, `useFormField`) were prefixed purely for family-consistency, not because they individually collided. Given Lumen's own `FormField` (a simple label/hint/error wrapper with no `react-hook-form`) is still actively used by `AuthForm.tsx` and doesn't need retiring, the user chose the middle option over both extremes offered: drop the prefix everywhere except the one real collision. `ShadcnFormField` keeps its name; everything else in the family is now plain. This is a third, distinct pattern from both §7.1 (full prefix) and §7.8 above (full promotion, original deleted) — worth remembering as its own option when a future family has a partial, not total, name overlap with an existing Lumen export.

---

# 7. Bulk adoption (2026-07-23 override)

Rule 3 above ("never duplicate an existing Lumen component") was explicitly overridden by direct user instruction on 2026-07-23, scoped to a bulk effort to adopt the entire shadcn `registry:ui` component set (61 components at the time), including ones that duplicate an existing Lumen primitive (e.g. shadcn's own `Button`, `Card`, `Tabs`). This section records that decision so it isn't mistaken for the default policy on a future component.

## 7.1 Naming collisions

Where a shadcn component's name exactly matches an existing Lumen public export (`Button`, `Card`, `Tabs`, `Tooltip`, `Select`, `Avatar`, `Input`, `Switch`, `Checkbox`, `Pagination`, and any other exact-name match discovered during this effort), the shadcn version is exported under a `Shadcn`-prefixed name (`ShadcnButton`, `ShadcnCard`, ...) rather than replacing or renaming the existing Lumen export. This was chosen specifically to be additive and non-breaking: existing Lumen exports and every current consumer of them are untouched. Where shadcn's own name doesn't collide (e.g. `Dialog` vs. Lumen's `Modal`, `RadioGroup` vs. Lumen's `Radio`, `Table` vs. Lumen's `DataTable`), the shadcn component keeps its own shadcn-native name — only genuine identical-name collisions get the prefix.

> **Superseded for all 11 of the above, 2026-07-24 (§7.8):** by direct user instruction, every one of the 11 `Shadcn`-prefixed duplicates named above was un-duplicated — Lumen's original component deleted, the shadcn-sourced version promoted to the plain name. This paragraph describes the *original* additive convention (still the right default for any *future* collision, unless re-litigated the same way); §7.8 is the record of why and how it was reversed for these specific 11.

## 7.2 What this does *not* change

Rules 1, 2, 4–10 in Section 4 still apply in full to every bulk-adopted component: no shadcn default theme values, full design/code/a11y review, Storybook coverage, dark mode via `[data-theme]` only, etc. The override is narrowly scoped to rule 3 (duplication) — it does not relax token discipline, testing requirements, or the internal/public split.

## 7.3 Batch tracking

Given the scope (~59 remaining components after `Command`/`Accordion`), this effort is delivered in small batches, each validated (typecheck/test/lint/Storybook build) and reported before the next batch starts, rather than as one bulk change — consistent with this repo's preference for one fully-integrated component over several partially-adapted ones, just applied per-batch instead of per-component. See `docs/changelog.md`'s `[Unreleased]` section for the batch-by-batch record.
