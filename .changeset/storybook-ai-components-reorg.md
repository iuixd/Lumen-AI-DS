---
"@lumen/ui": patch
---

Moved `AIPanel` and the new "AI Empty Communication State" story into Storybook's "AI Components" sidebar section (alongside the AI Button library), and switched Storybook's sidebar sort to alphabetical within every section. No component API changes — Storybook organization only.
