---
"@lumen/web-components": minor
"@lumen/angular": minor
---

Bring both packages to parity with `@lumen/ui`'s "AI ButtonGroup Component Library" sync: add `<lumen-segmented-control>`/`<lumen-segmented-control-option>` (`LumenSegmentedControlComponent`/`LumenSegmentedControlOptionComponent` in Angular), and add `tone`/`icon` to `<lumen-choice-chip>` (`LumenChoiceChipComponent`). Same design source and known limitations as the corresponding `@lumen/ui` components — see `docs/changelog.md`.
