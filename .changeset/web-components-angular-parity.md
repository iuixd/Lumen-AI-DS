---
"@lumen/web-components": minor
"@lumen/angular": minor
---

Bring both packages to parity with `@lumen/ui`'s 2026-07-14 Figma sync: add `status` to Button, and add `SplitButton`, `FilterChip`, `ChoiceChip`, and `AIButton`. Same design source and known limitations as the corresponding `@lumen/ui` components — see `docs/changelog.md`.
